-- Either learn search and replace with quickfix-list 
-- and create keybindings for it
--
-- OR
--
-- Make a plugin that makes this smooth


-- We should enable search in file with picker (maybe picker grep scoped to file)
